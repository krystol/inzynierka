<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="ak.inzynierka">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:usesCleartextTraffic="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity
                android:name=".core.pralnia.PralniaGodzina"
                android:label="@string/title_activity_pralnia_rezerwacja">
        </activity>
        <activity
                android:name=".core.pralnia.PralniaKalendarz"
                android:label="@string/title_activity_pralnia_kalendarz">
        </activity>
        <activity
                android:name=".core.MainActivity"
                android:label="@string/app_name">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            -->
        </activity>
        <activity
                android:name=".core.ogloszenia.OgloszeniaLista"
                android:label="@string/title_activity_ogloszenia"/>
        <activity
                android:name=".core.pralnia.PralniaLista"
                android:label="@string/title_activity_pralnia"/>
        <activity
                android:name=".core.salki.SalkaLista"
                android:label="@string/title_activity_lista_salek"/>
        <activity
                android:name=".core.salki.Salka"
                android:label="@string/title_activity_salka"/>
        <activity
                android:name=".core.ogloszenia.OgloszenieEdycja"
                android:label="@string/title_activity_ogloszenie"/>
        <activity
                android:name=".core.LoginActivity"
                android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>